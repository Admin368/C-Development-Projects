#includ<stdio.h>
int main(){
//string = array of char
//terminating char, last character
//to show end of string, just compasate space+1 in your array
//as it will be automatically added 
// value of the terminating character "\0"

 //declaring a string
 char myString[string_length] = " sting in quotes ";
 printf("this is the string %s",myString;

//x-rof more of the functions available in the <stdio.h>

//getchar
 char ch;
 ch - getchar(); //takes in input from keyboad
 while((ch=getchar())!='1'){
putchar(ch); //will put a character only and is unformated than printf
}

}